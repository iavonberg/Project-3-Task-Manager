import React from "react";

const NewTaskButton = props => {
    let style = {
        marginLeft: 10,
    }
    return (
        <button style={style}>Add Task</button>
    )
}

export default NewTaskButton;